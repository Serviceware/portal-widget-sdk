---
"@serviceware/portal-widget-cli": patch
---

The bundled `portal-private-widget` skill now mentions the experimental `ctx.service()` Service Connection
proxy. It offers it only for Portals running the backend proof of concept, and never as a production
solution.
